<?php
/**
 * Манифест привязок плагинов к системным событиям. Применение:
 *   php console/mpc.php events apply путь/к/events.php [--dry-run]
 *
 * ДЕКЛАРАТИВНО: для каждого перечисленного плагина набор привязок приводится
 * РОВНО к указанному списку — недостающие события привязываются, лишние
 * отвязываются. Плагины, не указанные в манифесте, не трогаются. Перед боевым
 * запуском прогоните --dry-run (список — это ПОЛНЫЙ желаемый набор событий!).
 */
return [
    'MyPlugin' => [
        'OnDocFormSave',
        'OnDocFormDelete',
        'OnHandleRequest',
    ],
];
