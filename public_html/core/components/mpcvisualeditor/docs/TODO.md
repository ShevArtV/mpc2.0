# mpcVisualEditor — TODO / роадмап

Оставшиеся работы по фронт-редактированию (mpcVE) и связанному бэкенду mpc.
Отмечено `[mpcVE]` / `[mpc]` — в каком пакете основная часть.

## Следующая сессия (приоритет, со слов пользователя 2026-06-03)
1. [x] **В режиме редактирования отключать переходы по ссылкам** `[mpcVE]` — ✅ M14: `bindClicks` гасит навигацию по любому `<a>` (маркированному и нет) в edit-mode (`e.target.closest('a')` → preventDefault).
2. [x] **Редактирование ссылок** `[mpcVE]` — ✅ M14: `data-mpc-field` на самом `<a>` → редактор `link` правит АДРЕС (href); ТЕКСТ ссылки — маркером на элементе ВНУТРИ (текст-редактор). Кнопки/атрибут-поля (`data-mpc-attr`) — ОТМЕНЕНО (со слов пользователя, не делаем).
3. [~] **Дофиксить редактирование медиа-файлов** `[mpcVE]` — ✅ M15/M15.1: video/audio (одиночные) — редактор `media.js` (файл, постер, булевы атрибуты controls/autoplay/loop/muted через УСЛОВНЫЙ рендер mpc 2.5.22-rc, preload-строка, источники `<source>`, загрузка через `kind`-aware `ImageUploadHandler`). picture был с M12. Осталось проверить на реальных данных + edge: пустой source-индекс vs ключи (mergeMediaRecord матчит sources по индексу — при reorder/delete возможен сдвиг ключей, как у picture).
4. [ ] **Фиксированный сайдбар со списком секций** `[mpcVE]`+`[mpc]` — в шаблоне; менять ПОРЯДОК (`position`), ВИДИМОСТЬ (`hide_section`), СТАТИЧНОСТЬ (`is_static`) секций. ⚠️ проверить, как смена статичности отработает (контент static→global vs resource — это меняет уровень хранения; нетривиально).
5. [ ] **Расширение верхней панели (тулбара)** `[mpcVE]` — кнопки: очистка кэша, переход в админку к текущему ресурсу, перенарезка секций БЕЗ обновления контента. *Нужно обсудить (моё мнение):* очистка кэша и «открыть в админке» — простые и безопасные, делать; «перенарезка без обновления контента» = `mgr_tpl` без `updContent` — мощно, но это деструктивная операция (перезаписывает chunks/parsed, см. память `no-full-recut-no-debug-render`), нужен confirm + чёткое понимание что именно перетрётся; обсудить scope в начале сессии.
6. [x] **Лексиконы пропадают после `mgr_tpl.php` до очистки кэша** `[mpc]` — ✅ mpc 2.5.23-rc: причина — грабер сбрасывал только `lexicon_topics`, но НЕ resource-кэш (закэшированные страницы держат старые резолвнутые `{key|lexicon}`). `Mpc::process` теперь в конце зовёт `refreshSiteCache()` (`lexicon_topics` + `resource` контекста), симметрично `FieldWriter::afterSave`. Осталось проверить на сайте (вкл. возможный pdoTools-кэш, если где-то `&cache=1` на сниппетах поверх resource-кэша).

## Редакторы контента
- [~] **Редактирование списков** `[mpcVE]` — v1 (M11) + вложенные списки и JS-рендер add (M11.6): клик по списку (вкл. `data-mpc-field-N`) → панель строк (add/↑/↓/✕), `row/op` по path → `ConfigFieldWriter` (mpc 2.5.19). Add клонирует первую строку с очисткой значений (плейсхолдеры), вложенная структура сохраняется (deep-clone) → дочерний список заполняем сразу; delete/move/add отражаются в DOM без автоперезагрузки (ручная кнопка «Обновить»). Осталось: ✅ drag-drop вместо ↑/↓ (M17). add в ПУСТОЙ список (нет DOM-шаблона — схема полей только на нарезке, пока зовём «Обновить» — функционально работает, оставляем); медиа-строки picture/video/audio в МЕДИА-списках (сейчас 📷 только для img; но медиа-СПИСКИ DEPRECATED, в обычных списках медиа-поле кликабельно на странице — развитие не ведём).
- [x] **Редактирование остальных медиа (ОДИНОЧНЫХ)** `[mpcVE]` — ✅ `picture` (M12: вложенный `img` + `sources`), ✅ `video`/`audio` (M15: атрибуты controls/autoplay/loop/muted/preload + постер + массив `<source>` с type/media, загрузка нескольких источников через `kind`-aware upload). Запись-aware для img/picture/video/audio. (Не путать с медиа-СПИСКАМИ ниже — DEPRECATED.) Осталось только полевое тестирование на реальных данных.
- ~~Медиа-СПИСКИ (`list_images`/`list_pictures`/…)~~ — **DEPRECATED** (M11.4): нарезаются фиксированными слотами, не `{foreach}`; роль закрыта произвольными именами полей секции (mpc 2.5.0). Легаси-редактирование (📷) оставлено, развитие не ведём.
- [~] **Редактирование скрытых полей** `[mpcVE]` — v1 СДЕЛАН (панель «Скрытые поля» в тулбаре, config-driven). Контролы ПО ТИПУ (M16.1): text→input, textarea→`<textarea>`, richtext→RTE с тулбаром (общий `editors/rte.js`), image→загрузка. Осталось: правка скрытых МЕДИА/записей (img/video/audio/picture) и вложенных записей прямо в панели — сейчас они пропускаются (нужен value-based редактор записей, не привязанный к DOM-элементу). Зависит от «Редактирование остальных медиа».

## Настройки и оформление
- [ ] **Настройки секции** `[mpcVE]`+`[mpc]` — видимость (`hide_section`) и порядок (`position`) секций прямо из фронта (drag/toggle), запись в `mpc_config`.
- [ ] **Стили секции** `[mpcVE]`+`[mpc]` — `inline_styles` / `class_names` / `css_file_path` (таб «Стили» конфигуратора) редактируемы из фронта.

## Интеграции
- [ ] **miniShop2 — редактирование товаров** `[mpcVE]`+`[mpc]` — БОЛЬШОЙ пласт: поля `msProduct` (цена, артикул, опции, галерея, св-ва), вывод товаров сниппетами (msProducts) → кросс-ресурсная правка (опереться на `data-mpc-res`), учёт msOptions/типов.

## Совместная работа / надёжность
- [ ] **Блокировка ресурса на время редактирования** `[mpcVE]` — чтобы двое не правили один ресурс одновременно. Снимать блокировку:
  - авто-выход из режима редактирования через N минут без изменений контента (idle-таймаут) → освобождение блокировки;
  - явный выход / закрытие вкладки (beacon).
- [ ] **Лог изменений (кто / когда / что)** `[mpcVE]`+`[mpc]` — *надо ли?* — журнал правок через редактор (адрес поля, старое/новое значение, пользователь, время). Решить необходимость и формат хранения.

## Сделано (для контекста)
- Произвольные имена полей секции (ftype/fcap/fdesc), динамические списки `mpc_auto_*`, прототипы типов в `mpc_base` — mpc 2.5.0.
- Редактор `img` (загрузка файла, alt/title, фоновые картинки), тип редактора по `data-mpc-ftype`.
- Лексикон-aware запись (текст/медиа-под-поля), лексиконизация полей ресурса (`rfield` → `mpc_resource_<field>`), кросс-ресурс `data-mpc-res` — mpc 2.5.1.
- Единый exclude для всех полей: `rfield` теперь проходит через `excludeLexiconFields` (грабер `ResourceFieldGrabber::lexiconize` + каттер `replaceResourceMarkers`, публичный `LexiconManager::isExcluded`); `pagetitle` в дефолтном списке (читается MODX напрямую → в колонке остаётся значение, не ключ). Другие нативные поля (`menutitle`/`longtitle`/…) — добавлять в project-specific exclude-файл при необходимости.
- Лексиконизация TV (`data-mpc-tv`) по аналогии с rfield: грабер пишет TV-значение=ключ + текст в лексикон, каттер рендерит `{'mpc_resource_tv_<name>' | lexicon}`, `FieldWriter::writeTv` правит лексикон-ключ. Отдельный неймспейс `mpc_resource_tv_` (не коллизит с rfield того же имени). Exclude и degradation (нет ключа / лексиконы off → прямой TV) — как у rfield. mpcVE-сторона (роутинг `type:'tv'` → writeTv) уже была — редактирование TV из фронта теперь лексикон-aware. mpc 2.5.3.
- Скрытые поля у блока (v1): кнопка «⊕ N» в углу секции / элемента списка → панель этого блока, config-driven. mpc 2.5.4 (`FieldWriter::readConfig`) + mpcVE M10 (`config/get`, `Mpcve::readConfig`, `ConfigGetHandler`, триггеры/панель в `mpcve.js`/`overlay.css`). Скалярные поля секции + скрытые под-поля строк списков (level-1), которых нет в DOM; запись прежним `field/save`. Медиа/записи пока пропускаются.
