# mpcVisualEditor — TODO / роадмап

Оставшиеся работы по фронт-редактированию (mpcVE) и связанному бэкенду mpc.
Отмечено `[mpcVE]` / `[mpc]` — в каком пакете основная часть.

## РОАДМАП следующей сессии (зафиксирован 2026-06-04, порядок приоритета)

1. [x] **Файловый менеджер в редакторе** `[mpcVE]`+`[mpc]` — ✅ M28: мини-менеджер на `modMediaSource` (`FileManagerHandler` + экшены `files/list|mkdir|rename|remove|upload`; фронт `filemanager.js` → `openFileManager({accept,title})`). Навигация (хлебные крошки + ↑), CRUD папок и файлов (создать папку, переименовать/удалить файл и папку), загрузка в текущую папку, переключатель источника (source-aware). Встроено «📁 Выбрать существующий» рядом с «Загрузить» в редакторах `image` (вкл. `bg`), `picture` (основная+источники), `media` (файл/постер/источники). Пути — относительные базе источника; права источника принудительно включаются после доверенной проверки коннектора (mpcve_edit), т.к. в web-контексте mgr-прав на файлы нет. `promptDialog` добавлен в `dom.js`. **ОСТАЛОСЬ:** интеграция в `file`-TV — в составе п.3 (TV-типы). Хвост для пакета: лексиконы `mpcve_fm_*` уже добавлены.

2. [ ] **Улучшение работы с лексиконами (экспорт/импорт)** `[mpc]` — УЖЕ есть: CMP + процессоры `export` (per-resource XLSX 2 листа Resource/Static), `exportall` (ZIP из XLSX), `import` (XLSX/ZIP), `getlist/get/getlanguages/updatekey`. ДОБАВИТЬ режим «всё одним файлом»:
   - **Экспорт:** ОДИН workbook, **вкладка-на-ресурс** (имя вкладки = identifier лексикон-файла), колонки `контекст | key | ru | en | …`. **Контекст** = название секции (`data-mpc-name`) + подпись поля (caption из конфигуратора, через манифест `mpc_tracked_fields`), НЕ парсинг ключа. Справочная колонка (на импорт не влияет).
   - **Импорт:** детект → превью → подтверждение. Детект на загрузке (по именам вкладок → план: вкладка→ресурс→N ключей, X новых/Y изменённых), БЕЗ записи. Превью: снять галочки с вкладок, ремап нераспознанных вручную, (опц.) дифф. Запись только после «Подтвердить» + сброс кэша. Старые режимы (per-resource/ZIP) оставить.
   - Рантайм-хранение `.inc.php` НЕ менять (нужно для каскада global→type→resource).

3. [ ] **Расширение поддержки ТВ-типов** `[mpcVE]`+`[mpc]` — сейчас в редакторе TV-типы кроме image/richtext/textarea/migx падают в `text`. Добавить:
   - **listbox/listbox-multiple** — реюз `editors/listbox.js`, но опции из `elements` самой TV (не data-mpc-values) → нужен бэк-резолвер опций TV; он же закроет отложенный **`@SELECT`-резолвер** секционных listbox (синергия).
   - **checkbox / number / date / tags** — дешёвые отдельные контролы.
   - **file** — через файловый менеджер (п.1).
   - **migx-TV** — большой кусок (вложенный грид), делать ТОЛЬКО если реально используется (не подменяется mpc-секциями).
   - image — уже есть.

4. [ ] **Рефакторинг** `[mpcVE]`+`[mpc]` — после фич.

5. [ ] **Тестирование и сборка** — полевые проверки (медиа edge: сдвиг `<source>`; миграция `is_static`; лексиконы после mgr_tpl) + сборочные хвосты: событие `OnBeforeDocFormSave` в build-декларацию плагина; резолверы таблиц `mpcve_changelog` и `mpc_tracked_fields` в build; авто-создание `lexicons-export/` и пр.

### Отложено (решение пользователя)
- **miniShop2 — редактирование товаров** — НЕ внедряем пока (title правится как поле ресурса; картинка=галерея; цена/опции/размеры правят редко и на этапе создания — в админке им норм). Может позже.

## Следующая сессия (приоритет, со слов пользователя 2026-06-04) — ДО товаров miniShop2
1. [x] **Нормальная модалка истории изменений** `[mpcVE]` — ✅ M26: `changelog.js` → модалка с таблицей (время/юзер/уровень/секция·поле/дифф/откат), фильтры секция→поле + источник (редактор/админка), сортировка по дате, инлайн-дифф по словам (del/ins), разворот полного контента. + М26 шаги 1-2: манифест трекаемых полей (таблица `mpc_tracked_fields`, на нарезке) и аудит правок из АДМИНКИ (`AdminAudit` + OnBefore/OnDocFormSave) — лог теперь полный (редактор+админка), `source`/`level` в записи. ОСТАЛОСЬ для пакета: добавить событие `OnBeforeDocFormSave` в build-декларацию плагина (на стенде зарегистрировано в БД).
   ~~переделать панель `changelog.js` в полноценную модалку с ТАБЛИЦЕЙ.~~ Нужно: (а) колонки (время, юзер, секция, поле, было→стало); (б) ФИЛЬТРЫ — по секции и по полю В ЭТОЙ секции (зависимый селект: выбрал секцию → список её полей); (в) СОРТИРОВКА по дате (asc/desc); (г) посмотреть КОНТЕНТ ЦЕЛИКОМ (сейчас обрезаем до 60/80 симв. — нужен раскрыв/модалка с полным old/new); (д) *огонь*: подсветка ДИФА (inline diff old↔new, напр. по словам/символам — зелёным добавленное, красным удалённое). Бэк `log/list` уже отдаёт записи; возможно добавить серверную фильтрацию/пагинацию, если записей много.
2. [x] **Атрибут `data-mpc-values`** `[mpcVE]`+`[mpc]` — ✅ M25: каттер вставляет значение в `inputOptionValues`, тип → `listbox`/`listbox-multiple` (`synthesizeScalar`); фронт-редактор `editors/listbox.js` (select/мультивыбор, `Caption==key` парсит, `@SELECT` — ручной ввод v1). M25.1: одиночный listbox лексиконизируется (капшены → `{prefix}_{field}_{key}`, плейсхолдер `##'..{$value}'|lexicon}`, значение=сырой ключ, save raw=1). Изначальный план «добавить в mpc_base» не понадобился — синтез типа в каттере. — передача ВОЗМОЖНЫХ значений поля из шаблона (для редактора-выпадайки). Прежде всего для НОВЫХ типов полей `listbox` и `listbox-multiple` — их надо ДОБАВИТЬ в `mpc_base` (прототипы типов) + редактор на фронте (select / multi-select). Значение атрибута — формат migx listbox: либо `Caption==key||Caption2==key2`, либо `@SELECT id,name FROM modx_site_content WHERE parent = 10`. ВАЖНО: НИЧЕГО не резолвить — грабер/каттер просто ВСТАВЛЯЮТ значение `data-mpc-values` КАК ЕСТЬ в нужное место migx-конфига поля (слот опций listbox), дальше migx сам всё разбирает (`@SELECT` и `Caption==key` — нативная фича migx). На фронте редактор берёт уже готовый список опций из конфигуратора.
3. [x] **Атрибут `data-mpc-max`** `[mpcVE]`+`[mpc]` — ✅ M25: каттер пишет `data-mpc-max` контейнера списка в `extended.maxRecords` авто-конфига (`buildListConfig`→`saveListConfig`); фронт `rows.js` блокирует «+ Добавить» при достижении лимита (читает атрибут из DOM). — ограничение МАКСИМАЛЬНОГО числа записей в списочных полях. migx это уже поддерживает (`max` в конфиге) — нужно просто прокинуть значение из шаблона (`data-mpc-max` на контейнере списка → в migx-конфиг поля) и учесть в редакторе строк (`rows.js`: не давать «+ Добавить», если достигнут max).

## Следующая сессия (приоритет, со слов пользователя 2026-06-03)
1. [x] **В режиме редактирования отключать переходы по ссылкам** `[mpcVE]` — ✅ M14: `bindClicks` гасит навигацию по любому `<a>` (маркированному и нет) в edit-mode (`e.target.closest('a')` → preventDefault).
2. [x] **Редактирование ссылок** `[mpcVE]` — ✅ M14: `data-mpc-field` на самом `<a>` → редактор `link` правит АДРЕС (href); ТЕКСТ ссылки — маркером на элементе ВНУТРИ (текст-редактор). Кнопки/атрибут-поля (`data-mpc-attr`) — ОТМЕНЕНО (со слов пользователя, не делаем).
3. [~] **Дофиксить редактирование медиа-файлов** `[mpcVE]` — ✅ M15/M15.1: video/audio (одиночные) — редактор `media.js` (файл, постер, булевы атрибуты controls/autoplay/loop/muted через УСЛОВНЫЙ рендер mpc 2.5.22-rc, preload-строка, источники `<source>`, загрузка через `kind`-aware `ImageUploadHandler`). picture был с M12. Осталось проверить на реальных данных + edge: пустой source-индекс vs ключи (mergeMediaRecord матчит sources по индексу — при reorder/delete возможен сдвиг ключей, как у picture).
4. [~] **Фиксированный сайдбар со списком секций** `[mpcVE]` — ✅ M19 (v1): кнопка «☰ Секции» → сайдбар со списком (сорт по `position`), drag-reorder (`position`), 👁 `hide_section`, 📌 `is_static`; всё `field/save` level=resource, эффект после «Обновить». Осталось проверить на данных: миграцию контента при смене `is_static`; секции только из type/donor-конфига (сейчас список — из ресурс-конфига).
5. [~] **Расширение верхней панели (тулбара)** `[mpcVE]` — ✅ M20: безопасные кнопки «🧹 Кэш» (полная очистка кэша MODX) и «⚙ Админка» (открыть ресурс в админке). Перенарезка-из-фронта — со слов пользователя НЕ делаем (деструктивна; если понадобится — отдельно с confirm).
6. [x] **Лексиконы пропадают после `mgr_tpl.php` до очистки кэша** `[mpc]` — ✅ mpc 2.5.23-rc: причина — грабер сбрасывал только `lexicon_topics`, но НЕ resource-кэш (закэшированные страницы держат старые резолвнутые `{key|lexicon}`). `Mpc::process` теперь в конце зовёт `refreshSiteCache()` (`lexicon_topics` + `resource` контекста), симметрично `FieldWriter::afterSave`. Осталось проверить на сайте (вкл. возможный pdoTools-кэш, если где-то `&cache=1` на сниппетах поверх resource-кэша).

## Редакторы контента
- [~] **Редактирование списков** `[mpcVE]` — v1 (M11) + вложенные списки и JS-рендер add (M11.6): клик по списку (вкл. `data-mpc-field-N`) → панель строк (add/↑/↓/✕), `row/op` по path → `ConfigFieldWriter` (mpc 2.5.19). Add клонирует первую строку с очисткой значений (плейсхолдеры), вложенная структура сохраняется (deep-clone) → дочерний список заполняем сразу; delete/move/add отражаются в DOM без автоперезагрузки (ручная кнопка «Обновить»). Осталось: ✅ drag-drop вместо ↑/↓ (M17). add в ПУСТОЙ список (нет DOM-шаблона — схема полей только на нарезке, пока зовём «Обновить» — функционально работает, оставляем); медиа-строки picture/video/audio в МЕДИА-списках (сейчас 📷 только для img; но медиа-СПИСКИ DEPRECATED, в обычных списках медиа-поле кликабельно на странице — развитие не ведём).
- [x] **Редактирование остальных медиа (ОДИНОЧНЫХ)** `[mpcVE]` — ✅ `picture` (M12: вложенный `img` + `sources`), ✅ `video`/`audio` (M15: атрибуты controls/autoplay/loop/muted/preload + постер + массив `<source>` с type/media, загрузка нескольких источников через `kind`-aware upload). Запись-aware для img/picture/video/audio. (Не путать с медиа-СПИСКАМИ ниже — DEPRECATED.) Осталось только полевое тестирование на реальных данных.
- ~~Медиа-СПИСКИ (`list_images`/`list_pictures`/…)~~ — **DEPRECATED** (M11.4): нарезаются фиксированными слотами, не `{foreach}`; роль закрыта произвольными именами полей секции (mpc 2.5.0). Легаси-редактирование (📷) оставлено, развитие не ведём.
- [~] **Редактирование скрытых полей** `[mpcVE]` — v1 СДЕЛАН (панель «Скрытые поля» в тулбаре, config-driven). Контролы ПО ТИПУ (M16.1): text→input, textarea→`<textarea>`, richtext→RTE с тулбаром (общий `editors/rte.js`), image→загрузка. ✅ M16.2: одиночные img-ЗАПИСИ value-based. ✅ M16.5: picture/video/audio-записи — кнопка «✎ Редактировать» открывает полный редактор value-based (`openPictureEditor`/`openMediaEditor` c `override`). Хвост закрыт (v1 — секционные записи; item-level nested пока нет).

## Настройки и оформление
- [x] **Настройки секции** `[mpcVE]`+`[mpc]` — ✅ M19: сайдбар «☰ Секции» (видимость `hide_section`, порядок `position` drag, `is_static`), запись в `mpc_config`.
- [x] **Стили секции** `[mpcVE]`+`[mpc]` — ✅ через панель скрытых полей (стилевая ветка, `SECTION_STYLE_FIELDS`): `inline_styles`, `class_names`, `props` («Дополнительные свойства») редактируемы из фронта; `css_file_path` НЕ правится (путь к файлу — `STRUCTURAL`). Каскад: правка на уровне ресурса перекрывает global.

## Интеграции
- [ ] **miniShop2 — редактирование товаров** `[mpcVE]`+`[mpc]` — БОЛЬШОЙ пласт: поля `msProduct` (цена, артикул, опции, галерея, св-ва), вывод товаров сниппетами (msProducts) → кросс-ресурсная правка (опереться на `data-mpc-res`), учёт msOptions/типов.

## Совместная работа / надёжность
- [x] **Блокировка ресурса на время редактирования** `[mpcVE]` — ✅ M21: лок в кэше MODX с TTL=`mpcve_lock_ttl` (idle-таймаут). acquire при входе (занято другим → баннер, правка off), heartbeat-продление пока активность, авто-выход по бездействию, release на «Завершить» + закрытии вкладки (beacon). `LockHandler` + `lock.js`.
- [x] **Лог изменений (кто / когда / что)** `[mpcVE]` — ✅ M22: таблица `mpcve_changelog`, захват field/save (+row/section аудит), кнопка «🕓 История» → панель, откат полей («↩ Вернуть»). Осталось: резолвер создания таблицы в build (сейчас вручную на стенде); откат медиа/структурных операций (пока только аудит).

## Сделано (для контекста)
- Произвольные имена полей секции (ftype/fcap/fdesc), динамические списки `mpc_auto_*`, прототипы типов в `mpc_base` — mpc 2.5.0.
- Редактор `img` (загрузка файла, alt/title, фоновые картинки), тип редактора по `data-mpc-ftype`.
- Лексикон-aware запись (текст/медиа-под-поля), лексиконизация полей ресурса (`rfield` → `mpc_resource_<field>`), кросс-ресурс `data-mpc-res` — mpc 2.5.1.
- Единый exclude для всех полей: `rfield` теперь проходит через `excludeLexiconFields` (грабер `ResourceFieldGrabber::lexiconize` + каттер `replaceResourceMarkers`, публичный `LexiconManager::isExcluded`); `pagetitle` в дефолтном списке (читается MODX напрямую → в колонке остаётся значение, не ключ). Другие нативные поля (`menutitle`/`longtitle`/…) — добавлять в project-specific exclude-файл при необходимости.
- Лексиконизация TV (`data-mpc-tv`) по аналогии с rfield: грабер пишет TV-значение=ключ + текст в лексикон, каттер рендерит `{'mpc_resource_tv_<name>' | lexicon}`, `FieldWriter::writeTv` правит лексикон-ключ. Отдельный неймспейс `mpc_resource_tv_` (не коллизит с rfield того же имени). Exclude и degradation (нет ключа / лексиконы off → прямой TV) — как у rfield. mpcVE-сторона (роутинг `type:'tv'` → writeTv) уже была — редактирование TV из фронта теперь лексикон-aware. mpc 2.5.3.
- Скрытые поля у блока (v1): кнопка «⊕ N» в углу секции / элемента списка → панель этого блока, config-driven. mpc 2.5.4 (`FieldWriter::readConfig`) + mpcVE M10 (`config/get`, `Mpcve::readConfig`, `ConfigGetHandler`, триггеры/панель в `mpcve.js`/`overlay.css`). Скалярные поля секции + скрытые под-поля строк списков (level-1), которых нет в DOM; запись прежним `field/save`. Медиа/записи пока пропускаются.
